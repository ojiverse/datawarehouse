{
  "name": "@ojiverse/query-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json && tsc -p test/tsconfig.json && tsc -p scripts/tsconfig.json",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy:dev": "wrangler deploy --env dev",
    "cf-typegen": "wrangler types",
    "smoke:real-r2sql": "node --experimental-strip-types scripts/smoke-real-r2sql.ts"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.13",
    "@cloudflare/workers-types": "^5.20260921.1",
    "@types/node": "^26.6.2",
    "typescript": "^5.9.0",
    "vitest": "^4.1.0",
    "wrangler": "^4.135.0"
  }
}
